// Firebase removed — app now uses PinGate password authentication.
// This file is kept as a stub so no other imports break during migration.

export const isFirebaseConfigured = false;
export const auth = null;
export const googleProvider = null;
export default null;
